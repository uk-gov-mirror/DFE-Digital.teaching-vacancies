h2.govuk-heading-m = t(".title")

- if publisher_signed_in?
  .manage-vacancies
    p #{t(".signed_in_as")} #{current_organisation.name}
    = govuk_link_to t(".manage_link"), organisation_path, class: "govuk-button--secondary manage-vacancies", button: true

- else
  p = t(".description")
  = govuk_link_to t("buttons.sign_in"), new_publisher_session_path, class: "govuk-button--secondary govuk-!-margin-bottom-0 identifications-button", button: true

  p = t(".signin_guidance_html", link: govuk_link_to(t(".signin_link"), new_publisher_session_path))
